# Implementation log — PR #6 remediation wave 2 (issues #020 #021 #023 #024 #027 #028 #029)

Date: 2026-06-11. Branch: experiment/opencode. Orchestrator: Claude Code (this log is the source of truth for the final deep-verification agent).

## Plan

All 7 issues are unblocked and file-disjoint. Constraint is runtime resources (resident tier server, RAM, launchd), not code deps.

- **Tranche A (parallel):** #020 (host/test/run-config-ab.sh), #021 (scripts/config-ab-normalized-ci.mjs + new test), #023 (harvest-runs-to-registry.mjs, config-ab-pairing-check.mjs, patch-context-overflow.mjs + tests), #024 (host/test/lib/registry_emit.js, runAgent.js + tests)
- **Coherence check A** (orchestrator): interfaces coherent, full containerized suite green at strictly higher count, registries untouched. Then commit (per-tranche convention).
- **Tranche B:** #027 (client/opencode/) ∥ #028 (verify-template.sh), then #029 (opencode-server lifecycle) alone — sequenced to avoid resident-tier/RAM/launchd contention.
- **Coherence check B** + commit.
- **Deep verification agent** pointed at this log; orchestrator re-verifies its findings independently.

Decisions made at kickoff:
- #021: symmetric route (reclassify overflow harness_error on BOTH sides), not flag+assert.
- Commits: per-tranche, T-style messages matching T2–T5 convention.
- Live verification: full, including launchd bad-GGUF crash-loop tests (user approved).
- Agents run targeted containerized tests; orchestrator runs the FULL suite once per tranche to avoid docker contention.

## Status

| Issue | Agent | Status | Notes |
|-------|-------|--------|-------|
| #020 | A1 → A1' | ✅ done, verified | A1' verified all 3 fixes, zero defects: trap testbed (SIGINT/SIGTERM → cleanup×1, exit 130, marker, no resume; clean path exit 0); space/newline path tree (4/4 summaries visited, OVERFLOW_RC survives — old loop visited 0); real containment guard exercised (`..` variants rejected rc=1, /tmp rejected by prefix check, legit + literal-dots paths proceed). Residual (in-spec): symlink escape not covered; SIGTERM exits 130 not 143; kill-to-driver-only defers trap until cell exits (bash semantics). |
| #021 | A2 → A2' | ✅ done, verified | A2' verified, zero defects: old-vs-new on BOTH committed registries differs only in the reclassified-label line — −5.47pp [−10.94, 0.00] and +0.78pp [−3.91, +5.86] byte-identical (committed registries have overflow harness_error only on claw-rig, 17 each). Both-sides fixture → symmetric −25.00pp (one-sided +8.33pp regression value negatively asserted). Arg validation char-identical to siblings, exit 2. 9/9 new tests green. Note: goldens against committed registry deliberately deferred to #025. |
| #023 | A3 | ✅ done, verified | 12 new tests green (patch 17/17, pairing 5/5, harvest-idempotency 4/4). Canonical registries green before+after, CIs reproduced verbatim. New contracts: pairing-check red shape `run_id=<id> lines n1, n2`; harvest summary gains `already_in_registry=`; patch JSON gains `registry_lines:[..]` (keeps `registry_line`). |
| #024 | A4 | ✅ done, verified | registry-emit-failure.test 2→7, runAgent.test 22→29, green in container. New harness_error values: `assertion_emit_failed` (NOTE: appears on terminal_status done/error/timeout rows, not harness_error status — tooling selecting by terminal_status won't see it; eligibility excluded via passed:null) and `post_script_spawn_failed` (conventional shape). registry_emit now promotes any string summary.harness_error onto harness_error-status rows. Precondition spawn failure emits NO row (caught by #003 expected-attempts audit). |
| #027 | B1 | ✅ done, verified | All 6 ACs pass. bin/oc: `assert_workspace()` (cd -P resolve; refuses /, $HOME, any mount point via device-number test; OC_ALLOW_BROAD_WORKSPACE=1 override w/ WARNING); remote pairing check in assert_server BEFORE health probe (ordering load-bearing: green() curl has no --max-time); probe mock /health route + bounded curl poll (100×0.2s) + OC_PROBE_MOCK_DELAY_MS test knob (old sleep-1 shape failed 2/2 under 3s delay, new passes 2/2). Dockerfile: ARG NODE_MAJOR=24 (matches prior image v24.16.0) + build assertion. Image rebuilt (56s, sha 52670a99cbd9); probe ~1.3s green; one-shot run green vs resident; wizard step 61 standalone rc=0. Known intermittent opencode startup wedge (rc=124) seen in harness construction, predates change. |
| #028 | B2 | ✅ done, verified | All 4 ACs pass; verifier-only (templates untouched, no render bug found). M_TOOLCALL_STRARGS fixture (string args) + 4 checks → 17/17 on BOTH qwen36 + qwen35 corrected templates, passing via minja requires_object_arguments polyfill (the pin the issue wanted). cleanup() now mirrors cmd_stop #005 drain (SIGTERM → kill-0 ≤STOP_TIMEOUT → SIGKILL → 10×1s lsof drain); --diff ×3 zero port-busy. mktemp scratch (RJSON/POUT) + trap cleanup; concurrent full boots (18080/18081) clean w/ distinct paths. Stale header nit: still says claw :11435 (flagged, out of scope). |
| #029 | B3 | ✅ done, verified | All 7 ACs pass. opencode-server: running_pid() identity check (ps command= must match $BINARY + --port; mismatch = stale pidfile removed, process untouched — proven with planted live sleep PID); per-tier mkdir lifecycle lock (rotate-script G3 pattern + dead-owner reclaim) at dispatch for start/stop/restart/install/uninstall (single acquisition → install's internal cmd_stop can't deadlock); cmd_stop #005 drain UNTOUCHED (verify-template copy in sync — orchestrator grep-verified). sed render → render_plist() python literal substitution w/ XML escaping + plutil -lint gate (`&`/`|`/`<`/`>` path byte-correct). validate-tool-calls.sh: REPEATS positive-int guard (0/-1/abc exit 1; REPEATS=1 full battery PASS 6/6 vs resident). Plists: KeepAlive={Crashed,SuccessfulExit:false} + ThrottleInterval=60 (bad-GGUF test label: 2 attempts in ~98s vs ~10 under old shape; booted out). Wizard 51: red branch polls ≤WIZARD_OC_HEALTH_WAIT then rc=1 with last 12 log lines (bad-GGUF rc=1 verified); 61 comment-only sentinel-oracle update (orchestrator verified comment-only). All test launchd state/locks/pidfiles cleaned; resident :11436 healthy, oc probe green. |

## Events

- 2026-06-11: Read all 7 issues; confirmed disjoint file surfaces. User approved per-tranche commits + full live verification. Launched Tranche A agents.
- Tranche A hit platform instability (safety-classifier outages reported by A3/A4; A1 paused; A2's completion lost). A3/A4 reported fully. Orchestrator inspected git diff: #020 and #021 implementations complete on disk but unverified. User rebooted harness; resumed.
- A3 note for tranche-end: baked `mac-llm-lab-test:local` image is stale vs HEAD — `docker compose build` (or live mounts) needed before full-suite run.
- Launched A1'/A2' verification-only agents for #020/#021. (Relaunched post-reboot; both returned clean.)
- **Coherence check A (orchestrator), all green:**
  - Diff surface exactly matches Tranche A scope (10 modified + 5 new test/fixture files, all under host/test/).
  - Cross-issue interfaces: driver consumes patch output via `"overflow":true` substring + exit code (intact; #023 kept `registry_line` alongside new `registry_lines`); pairing-check consumed via exit code only; #021 reclassification keys on `terminal_status==='harness_error' && harness_error==='context_overflow'` — no collision with #024's `assertion_emit_failed` (lives on done/error/timeout rows) or `post_script_spawn_failed` (different string).
  - Full containerized suite run by orchestrator: **143 tests, 142 pass, 0 fail, 1 pre-existing skip** (live-mounted sources via compose `test` service, stale-image concern moot).
  - Committed canonical registries untouched (no registry files in diff).
- Issue files #020/#021/#023/#024 marked ✅ Done, ACs ticked. Committed Tranche A as T6 (15472fd).
- Tranche B ran: B1 (#027) ∥ B2 (#028), then B3 (#029) solo. All agent reports above.
- **Coherence check B (orchestrator), all green:**
  - Diff surface exactly matches Tranche B scope (client/opencode/{Dockerfile,bin/oc}, opencode-server, validate-tool-calls.sh, 2 plists, verify-template.sh, wizard 51 + 61).
  - `bash -n` clean on all 6 changed shell files; `plutil -lint` OK on both plists.
  - wizard/steps/61 verified comment-only (no non-comment changed lines).
  - cmd_stop drain: the 6 drain-keyword diff lines are comments + lock owner-liveness + running_pid() rewrite — drain loop itself untouched, verify-template copy in sync.
  - Live: resident :11436 `{"status":"ok"}`; orchestrator-run `oc probe` PASS (sentinel captured).

## Known follow-ups (not blocking, surfaced to user)

1. **Deployed resident plist is still the old shape** (unconditional KeepAlive, throttle 10): re-rendering requires `opencode-server install`, which bootouts the resident — deliberately NOT done mid-workflow. Takes effect at next install.
2. Wizard step 61's `ok` message string (~line 64) still says "attribution line" — the gate is the sentinel since #014; one-line follow-up (61 was comment-only per #029 scope).
3. verify-template.sh header still references claw :11435 (stale post-claw-retirement; flagged by B2, out of verifier-behavior scope).
4. #020 containment guard does not cover symlink escapes (the spec explicitly accepted `..`-rejection as sufficient); residual exposure noted by A1'.
5. #021 goldens against the committed registry deliberately deferred to #025 ("coordinate with #021" note there).
6. B1 observed an intermittent opencode startup wedge (rc=124, ~90s, no request sent) twice during AC4 harness construction — predates these changes, known startup-stall family.

## Deep verification (final)

Deep-verification agent re-verified everything adversarially against this log + both commits. **Overall: CONFIRMED-WITH-FINDINGS — all seven implementations real, every spot-checkable number in this log reproduced exactly.** Its independent re-runs: tier-16 −5.47pp [−10.94, 0.00] and sidecar-port +0.78pp [−3.91, 5.86] exact; canonical registries pairing-check green ×3 with run_id uniqueness OK; oc-from-$HOME refusal rc=1; oc probe PASS; resident healthy; REPEATS=0/-1 guard rc=1; both plists lint OK; wizard 61 comment-only; cmd_stop drain untouched; no .jsonl outside test fixtures in either commit; no stray launchd/pidfile/lock state.

**Material finding (against the orchestrator, not the agents): the compose `test` service does NOT live-mount sources** — it runs baked /test with only .claw-runtime mounted, and the baked image predated the wave. The orchestrator's (and A2''s) "143/142/1" suite runs therefore executed pre-wave code; the coincidentally identical count masked it, and A3's stale-image warning was wrongly declared moot. The verifier's live-mounted run showed the true HEAD suite: **332 tests / 331 pass / 0 fail / 1 pre-existing skip**.

Orchestrator remediation (verified): confirmed the compose-mount claim by reading docker-compose.yml; ran `docker compose build test`; fresh-baked `docker compose run --rm test` → **332/331/0 fail/1 skip**, matching the verifier's live run. Stale-image follow-up resolved. Also cleaned the 16 leftover tplverify scratch logs in $TMPDIR (#028 nit; the $LOG mktemp not being in the verifier's trap is a pre-existing pattern, left as-is). Issue files 022/025/026 (referenced by this log but previously untracked) committed in the housekeeping commit.

## Follow-up wave (2026-06-11, after deep verification): follow-ups + Copilot PR #6 feedback

User requested: address the known follow-ups and the 4 Copilot review comments on PR #6. Agents C1/C2/C3 (disjoint surfaces), orchestrator did README nit + verification.

Copilot comment dispositions:
- **3393809758 (normalized-ci --tier)**: right problem, wrong prescription — post-#021 the script is the general symmetric tool, so `--tier 16` is NOT hard-required; instead `--tier` is now required + validated (missing/non-numeric/non-positive → exit 2 + usage), heading templated `tier-${tier}` (byte-identical for 16). +3 tests (12 total in file). Old-vs-new byte-identical on both committed registries (−5.47pp, +0.78pp).
- **3393809804 (tester CMD models)**: fixed — `CMD ["docker"]` (wizard's documented default; no alias for the retired mode). Rebuilt; bare `docker compose run --rm tester` green.
- **3393809822 (step 61 empty oport)**: fixed — explicit empty-port guard, fail+return 1 naming the cause; verified via stubbed-state harness (rc=1) and full-local step 61 green end-to-end.
- **3393809844 (curl|bash Node)**: fixed — base switched to `node:${NODE_MAJOR}-bookworm-slim` (ARG=24), Nodesource script gone; version assertion now guards base-tag/ARG agreement. Rebuilt: node v24.16.0, opencode 1.16.2, oc probe PASS, one-shot run rc=0 vs resident.

Follow-ups closed:
- #2 wizard 61 ok-message → sentinel wording (verified visible in green step-61 run).
- #3 verify-template: header → resident :11436; $LOG cleaned on success / kept+path-printed on failure (verified both ways; 17/17 still green).
- #4 symlink containment: physical resolution (`cd -P && pwd -P`, bash-3.2-safe) of CLAW_RT_DIR + registry parent after the kept `..`/prefix checks; mkdir-then-resolve for missing parent (matches lib/registry.js mkdirSync flow); plus `-L` reject for symlink FILES (agent addition, orchestrator accepted — same escape class). Verified: symlink dir + symlink file rejected, nested legit path proceeds, `..` cases unchanged, default path unchanged.
- oc localhost predicate: `server_host_is_local` is now the single predicate in assert_server; non-canonical loopback spellings get a health-check-only sub-branch preserving today's outcomes (never auto-boots). 192.0.2.1 fail-fast byte-identical.
- README stale base-image line fixed (orchestrator).

Suite after follow-up wave (freshly rebuilt image): **335 tests / 334 pass / 0 fail / 1 pre-existing skip**.

Follow-up #1 (resident plist redeploy): user explicitly authorized the bounce after T8. `opencode-server install` ran clean — bootout, re-render via render_plist + lint, bootstrap, /health green, full #010 admission probe (template invariants + tool-call battery 6/6, 0 leaks). Deployed plist verified new-shape: KeepAlive={Crashed:true, SuccessfulExit:false}, ThrottleInterval=60. Post-bounce `oc probe` PASS. ALL follow-ups from this wave now closed except the deliberately-deferred #025 goldens (that issue's own scope) and the pre-existing startup-wedge flake family (tracking note only).

Remaining open follow-ups after deep verification: #1 (deployed resident plist re-renders at next install), #2 (wizard 61 ok-message string), #3 (verify-template claw-:11435 header), #4 (symlink containment residual), #5 (#025 goldens), #6 (startup-wedge flake family), plus two new nits: verify-template's $LOG mktemp not trap-cleaned (pre-existing), and oc's localhost local/remote predicate asymmetry (benign, consistent outcome).
